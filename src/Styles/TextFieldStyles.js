import { StyleSheet } from "react-native";

export const textFieldStyles = StyleSheet.create({

    fieldGroup: {
        marginTop: 10,
        marginBottom: 10,
    },
    fontLarge: {
        fontSize: 18,
    },
    normalTextField: {
        borderColor: '#777',
        borderWidth: 1,
        marginTop: 5,
        height: 60,
        fontSize: 18,
        borderRadius: 10,
    }
});
